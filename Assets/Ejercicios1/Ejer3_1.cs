using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejer3_1 : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] int numeroEntero1 = 9;
    [SerializeField] int numeroEntero2 = 4;
    void Start()
    {
        Debug.Log("Numero entero : " + numeroEntero1);
        Debug.Log("Numero entero : " + numeroEntero2);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
